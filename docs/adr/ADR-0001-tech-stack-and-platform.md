# ADR-0001: Технологический стек и целевая платформа

Статус: Принято
Дата: 2026-08-19
Автор: согласовано с заказчиком в ходе проработки ТЗ
Затронутый EPIC: EPIC-001

## Контекст

Программа устанавливается на рабочий ноутбук сотрудника(ов) HR-отдела,
работает офлайн, без сервера. Нужно выбрать ОС, язык, UI-фреймворк, СУБД и
способ поставки, зафиксировав их как незыблемые (см. `ANCHOR_CORE.md`, п. A1–A3).

## Рассмотренные варианты

1. **Python + PySide6 + SQLite (выбрано).** Нативный десктоп, зрелая экосистема,
   быстрая разработка, ноутбук уже на Linux.
2. Electron + JS/TS + SQLite. Более тяжёлый рантайм, оверинжиниринг для
   офлайн-инструмента одного отдела.
3. Excel + VBA-макросы. Минимальный порог входа, но не тянет ролевую модель,
   шифрование БД, журнал действий и механизм произвольных шаблонов — отклонено
   как заведомо не покрывающее ТЗ целиком.

## Решение

- ОС: Linux, семейство Debian/Ubuntu (подтверждено заказчиком — реальный
  ноутбук HR на этой платформе).
- Язык: Python 3.
- UI: PySide6 (Qt 6), без браузера и локального веб-сервера.
- БД: SQLite + шифрование на уровне файла (SQLCipher или эквивалент, см.
  ADR-0002).
- Поставка: нативный пакет дистрибутива (.deb или аналог), без ручной
  установки Python/библиотек пользователем.

## Последствия

- Любая задача, предлагающая другой стек (например, веб-интерфейс на
  localhost, другую СУБД), требует новой ADR, не просто комментария в PR.
- CI и dev-окружение ориентируются на Linux; тестировать под Windows/macOS не
  требуется и не является критерием приёмки.
- Библиотека для XLSX зафиксирована дополнением EPIC-009. PDF для
  стандартных отчётов — дополнением EPIC-010 ниже. Заполнение чужих PDF
  (EPIC-011) — отдельный выбор.

## Как проверяется

EPIC-001: приложение собирается и запускается на целевой Linux-системе без
Windows/Wine/VM (соответствует разделу 11 ТЗ, блок «Безопасность и роли»,
первый пункт, и разделу 8).

---

## Дополнение: XLSX/CSV для импорта и экспорта сотрудников

Статус: Принято (завершает отложенный выбор из исходной ADR, не отменяет её)
Дата: 2026-08-27
Затронутый EPIC: EPIC-009 (повторно используется в EPIC-011)

### Контекст

Исходная ADR откладывала выбор Excel/PDF-библиотеки («openpyxl и аналоги»)
до EPIC-011. EPIC-009 требует чтение и запись XLSX/CSV раньше, чем появится
движок шаблонов. ТЗ §7 уже указывает `openpyxl` для импорта/экспорта.
Это случай `ANCHOR_PROTOCOL.md` §4.3 — зависимость фиксируется до кода.

### Решение

- **XLSX:** `openpyxl` — как в ТЗ §7 и как пример в исходной ADR. Сырой
  список сотрудников (не шаблонный отчёт).
- **CSV:** модуль стандартной библиотеки `csv`. Отдельная CSV-библиотека
  не нужна.
- **EPIC-011** использует тот же `openpyxl` для Excel-шаблонов. Второй
  Excel-стек не открывать, пока форматирование/формулы шаблона не докажут,
  что `openpyxl` недостаточен. PDF по-прежнему отдельный выбор в EPIC-011.

### Последствия

- `openpyxl` — runtime-зависимость в `pyproject.toml`, не dev-only.
- Импорт/экспорт сотрудников и будущие Excel-шаблоны делят одну библиотеку.

---

## Дополнение: PDF для стандартных отчётов

Статус: Принято (завершает отложенный выбор PDF, не отменяет исходную ADR)
Дата: 2026-08-27
Затронутый EPIC: EPIC-010

### Контекст

Дополнение EPIC-009 зафиксировало `openpyxl` и оставило PDF до EPIC-011,
предполагая, что PDF появится только в движке шаблонов. EPIC-010 требует
«Экспорт в Excel и PDF» для пяти стандартных форм — на один эпик раньше.
Это снова `ANCHOR_PROTOCOL.md` §4.3. Задача здесь — табличная вёрстка
с нуля, не подстановка в PDF заказчика с полями формы.

### Рассмотренные варианты

1. **reportlab** — чистый Python, офлайн, Linux, табличные PDF. Не заполняет
   чужие PDF-формы.
2. WeasyPrint — HTML/CSS, тянет системные GTK/Pango, тяжелее для .deb.
3. fpdf2 — тоже подходит для таблиц; менее зрелая экосистема, чем reportlab.

### Решение

- **Стандартные отчёты (этот эпик):** `reportlab` — генерация табличного PDF.
- **EPIC-011 этим выбором не закрывается.** Шаблоны заказчика (именованные
  поля формы / координатные области) — другой класс задач. `ROADMAP.md`
  уже отмечает неопределённость «как реальные PDF-файлы заказчика себя
  поведут». Для заполнения чужого PDF, скорее всего, понадобится отдельная
  библиотека (например pypdf/pdfrw) или отказ на загрузке, если файл
  неподдерживаемый. Переиспользовать reportlab в EPIC-011 только если
  окажется, что достаточно генерировать PDF с нуля, а не править исходник
  заказчика.

### Последствия

- `reportlab` — runtime-зависимость в `pyproject.toml`.
- Кириллица требует системный TTF (DejaVu на целевом Linux / CI).

---

## Дополнение: заполнение PDF-шаблонов заказчика (EPIC-011)

Статус: Принято (детали контракта — **ADR-0005**, не дублировать здесь)
Дата: 2026-08-29
Затронутый EPIC: EPIC-011

### Решение

- Заполнение **чужого** PDF (AcroForm, координаты) — **`pypdf`** + overlay
  **`reportlab`** по ADR-0005.
- Генерация табличных PDF «с нуля» (EPIC-010) по-прежнему только **reportlab**.
